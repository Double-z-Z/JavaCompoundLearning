# 当前任务清单

> **目标**: 按术语表完成代码层命名对齐，消除文档与代码偏差。
> **计划依据**: `03-代码对齐计划.md`
> **防退化原则**: 执行前必须阅读 `03-代码对齐计划.md` 的「防退化六条」

---

## Phase 1: 重命名隔离类型（RuntimeType → IsolationType）

- [ ] **1.1** 重命名文件 `RuntimeType.java` → `IsolationType.java`，枚举名同步修改
- [ ] **1.2** 全局替换 `RuntimeType` 引用（含 import、字段类型、方法参数、局部变量）
- [ ] **1.3** 更新 `EnvironmentSpec` 中 `runtime` 字段为 `isolationType`，类型改为 `IsolationType`
- [ ] **1.4** 更新 `Environment` 实体中 `runtime` 字段为 `isolationType`
- [ ] **1.5** 更新 `EnvCreateRequest` 中 `runtime` 字段为 `isolationType`
- [ ] **1.6** 更新 `EnvironmentHandler`：`parseRuntimeType` → `parseIsolationType`，所有 `.getRuntime()` → `.getIsolationType()`
- [ ] **1.7** 更新所有测试文件中的 `RuntimeType.DOCKER/NATIVE` → `IsolationType.DOCKER/NATIVE`
- [ ] **1.8** 执行验证：编译 + 测试通过

---

## Phase 2: 重命名环境业务类型（type → environmentType）

- [ ] **2.1** 更新 `EnvCreateRequest`：`type` 字段 → `environmentType`
- [ ] **2.2** 更新 `EnvironmentSpec`：`type` 字段 → `environmentType`
- [ ] **2.3** 更新 `Environment` 实体：`type` 字段 → `environmentType`
- [ ] **2.4** 更新 `EnvironmentHandler`：所有 `request.getType()` / `spec.getType()` → `request.getEnvironmentType()` / `spec.getEnvironmentType()`
- [ ] **2.5** 更新 `EnvironmentHandler.envList()` 响应构建：第 213 行 bug 修复，`"type"` 键改为 `"environmentType"` 和 `"isolationType"`
- [ ] **2.6** 更新所有测试中的构造器和断言
- [ ] **2.7** 执行验证：编译 + 测试通过

---

## Phase 3: 新增运行时版本约束（runtimeConstraint）

- [ ] **3.1** `EnvCreateRequest` 新增 `runtimeConstraint` 字段（String，可选）
- [ ] **3.2** `EnvironmentSpec` 新增 `runtimeConstraint` 字段（String，可选）
- [ ] **3.3** `Environment` 实体新增 `runtimeConstraint` 字段（String，可为 null，DB 列 `runtime_constraint`）
- [ ] **3.4** `EnvironmentHandler.buildSpec()` 新增 `.runtimeConstraint(request.getRuntimeConstraint())`
- [ ] **3.5** 更新 `Environment.create()` / `provisioned()` 工厂方法，赋值 `runtimeConstraint`
- [ ] **3.6** 新增测试：验证 `runtimeConstraint` 可选，不传时不报错；传入时正确保存到实体
- [ ] **3.7** 执行验证：编译 + 测试通过

---

## Phase 4: 重命名服务名（templateName → serviceName）

- [ ] **4.1** 更新 `EnvDeployRequest`：`templateName` 字段 → `serviceName`
- [ ] **4.2** 更新 `ServiceManifest`：`templateName` 字段 → `serviceName`，`fromTemplate` → `fromServiceName`
- [ ] **4.3** 更新 `EnvironmentHandler.envDeployService()`：所有 `request.getTemplateName()` → `request.getServiceName()`，`templateName(...)` → `serviceName(...)`
- [ ] **4.4** 检查 `EnvOperationResponse` 及 Builder，如有 `templateName`/`template` 字段则改为 `serviceName`
- [ ] **4.5** 更新所有测试中的构造器和断言
- [ ] **4.6** 执行验证：编译 + 测试通过

---

## Phase 5: 清理与验证

- [ ] **5.1** 更新所有 Javadoc 和注释中的旧命名（`type`、`runtime`、`templateName`、`RuntimeType`）
- [ ] **5.2** 更新 `EnvironmentHandler` 日志字符串中的旧参数名
- [ ] **5.3** 执行 grep 清零验证（详见 `03-代码对齐计划.md` 第五节）
- [ ] **5.4** `./mvnw clean test` 全量测试通过（当前基线 58 个测试）
- [ ] **5.5** 人工抽查：启动 MCP Server，用 curl 调用 `env_create` 和 `env_list`，确认响应 JSON 字段名与 `02-MCP-Tools.md` Schema 完全一致

---

## 历史已完成的任务（仅留档，勿重复执行）

- [x] V3 设计文档重写（统一语言 + 术语表 + 架构图）
- [x] V2/V3 归档整理（CHANGELOG / HISTORY 拆分）
- [x] 删除已废弃的详细设计文档
- [x] 删除 REST API 相关文件和内容

---

*本清单随代码对齐进度实时更新。每完成一个 Phase，在对应复选框打勾并提交 commit。*
