# DevOps MCP Server — 整体设计

> 文档编号: DES-MCP-001
> 依赖: ADR-MCP-001 ~ ADR-MCP-010

---

## 1. 架构全景

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              AI Host 层                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  MCP Client (Claude Desktop / Trae / Kimi)                              │  │
│  │  • 解析用户自然语言意图                                                 │  │
│  │  • 调用 MCP Tools / 读取 Resources                                     │  │
│  │  • 多轮对话，逐步确认                                                   │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ MCP Protocol (stdio / SSE / HTTP)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          MCP Server 层 (新增)                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │   MCP Adapter   │  │   MCP Router    │  │      Prompt Registry        │  │
│  │  (协议适配)      │  │  (Tool/Resource │  │  • experiment_wizard        │  │
│  │                 │  │   路由分发)      │  │  • ops_troubleshooter       │  │
│  └─────────────────┘  └─────────────────┘  │  • network_awareness        │  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │  │
│  │                    MCP Tool Handlers                                 │   │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │   │  │
│  │  │Discovery │ │Environment│ │ Testing  │ │ Diagnosis│ │ Evidence │ │   │  │
│  │  │ Handlers │ │ Handlers  │ │ Handlers │ │ Handlers │ │ Handlers │ │   │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │   │  │
│  └─────────────────────────────────────────────────────────────────────┘   │  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ 内部调用 (Mono/Flux)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        应用服务层 (现有，保持不变)                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ HostService  │  │Environment   │  │ Template     │  │ Experiment       │  │
│  │ (新增)       │  │ Service      │  │ Library      │  │ Service          │  │
│  │ • 节点注册    │  │ (现有)       │  │ (现有)       │  │ (现有，弱化蓝图)  │  │
│  │ • 拓扑查询    │  │ • CRUD      │  │ • 模板查询   │  │ • 实验意图记录   │  │
│  │ • 资源统计   │  │ • 部署      │  │ • 参数元数据  │  │ • 证据聚合      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                        │
│  │ Loadgen      │  │ Monitoring   │  │ Evidence     │                        │
│  │ Service      │  │ Service      │  │ Collector    │                        │
│  │ (新增)       │  │ (现有)       │  │ (新增)       │                        │
│  │ • SSH远程执行 │  │ • 指标采集   │  │ • 结构化存储 │                        │
│  │ • 压测调度   │  │ • 日志流     │  │ • 关联查询   │                        │
│  └──────────────┘  └──────────────┘  └──────────────┘                        │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        领域层 & 基础设施层 (现有)                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Host         │  │ Environment  │  │ Service      │  │ Infrastructure│    │
│  │ (新增聚合根)  │  │ (现有聚合根)  │  │ Template     │  │ Provider      │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│  ┌──────────────┐  ┌──────────────┐                                          │
│  │ HostRegistry │  │ Experiment   │                                          │
│  │ (仓储)       │  │ Repository   │                                          │
│  └──────────────┘  └──────────────┘                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 核心概念模型

### 2.1 Host（主机节点）— 新增核心概念

Host 是基础设施的**物理/逻辑节点**，高于 Environment。

```java
public class Host {
    private final HostId id;
    private final HostType type;           // PVE_HYPERVISOR, VM, BARE_METAL, LOCAL
    private final HostId parentId;         // null if root
    private final String label;
    private final NetworkZone networkZone; // 如 "lan-10.0.0"
    private final Set<Capability> capabilities; // DOCKER, NATIVE, VM
    private final Set<HostRole> roles;     // MCP_HOST, TARGET, LOADGEN
    private final ResourceCapacity resources;
    private final AccessInfo access;       // SSH 连接信息
    private final Set<LoadgenTool> loadgenTools; // 仅 LOADGEN 角色有效
    private final HostStatus status;
}

public enum HostType {
    PVE_HYPERVISOR,   // PVE 宿主机，parent=null
    VM,               // 虚拟机，parent 指向 PVE_HYPERVISOR
    BARE_METAL,       // 裸金属，parent=null
    LOCAL             // 本地开发机，parent=null
}

public enum HostRole {
    MCP_HOST,         // 运行 MCP Server 进程
    TARGET,           // 可部署环境/服务
    LOADGEN           // 可执行压测
}

public enum Capability {
    DOCKER,           // 运行 Docker Daemon
    NATIVE,           // 可直接运行进程/二进制
    VM                // 可创建虚拟机（仅 PVE_HYPERVISOR）
}
```

### 2.2 Environment（环境）— 增强归属

Environment 必须显式绑定到 Target Host。

```java
public class Environment {
    private final EnvironmentId id;
    private final HostId hostId;           // 新增：明确归属哪个 Target Host
    private final EnvironmentType type;    // DEV, TEST, STAGING, EXPERIMENT
    private final RuntimeType runtime;     // DOCKER, NATIVE
    private final EnvironmentStatus status;
    private final List<ServiceInstance> services;
    private final ResourceQuota resourceQuota;
    private final LifecyclePolicy lifecyclePolicy;
    private final Map<String, Endpoint> accessEndpoints;
}
```

### 2.3 ExperimentSession（实验会话）— 替代 ExperimentRun

一次用户与 AI 的实验交互产生一个 Session，用于聚合证据。

```java
public class ExperimentSession {
    private final SessionId id;
    private final String intent;           // 用户原始意图文本
    private final HostId targetHostId;     // 实验部署的目标 Host
    private final HostId loadgenHostId;    // 压测使用的 Host
    private final EnvironmentId environmentId;
    private final SessionStatus status;
    private final List<Evidence> evidenceLog;
    private final Conclusion conclusion;
    private final Instant createdAt;
    private final Instant archivedAt;
}

public enum SessionStatus {
    PLANNING,       // 用户与 AI 对话规划中
    PROVISIONING,   // 创建环境中
    DEPLOYING,      // 部署服务中
    VERIFYING,      // 功能验证中
    LOAD_TESTING,   // 压测中
    COLLECTING,     // 收集证据中
    CONCLUDED,      // 已提交结论
    ARCHIVED        // 已归档
}
```

---

## 3. 数据流设计

### 3.1 标准实验流程数据流

```
用户: "我要验证 Nacos 单机 1000 QPS"
  │
  ▼
AI Client
  │ 1. 调用 Resource: hosts://topology
  │ 2. 调用 Resource: templates://list
  ▼
MCP Server 返回拓扑和模板
  │
  ▼
AI 向用户呈现选项：
  "可用 Target Host: [A] vm-fedora-dev [B] vm-ubuntu-test"
  "可用 Loadgen Host: [A] vm-loadgen-01"
  │
  ▼
用户: "选 B 作为 Target，A 作为 Loadgen"
  │
  ▼
AI 调用 Tool: analyze_network_path
  (source=vm-loadgen-01, target=vm-ubuntu-test, port=8848)
  │
  ▼
MCP Server 返回路径分析
  │
  ▼
AI 向用户提示："同 PVE 宿主机内通信，未过物理网卡"
  │
  ▼
用户: "确认，先这样测"
  │
  ▼
AI 调用 Tool: env_create (target_host_id=vm-ubuntu-test)
  │
  ▼
MCP Server → EnvironmentService.createFromSpec()
  │
  ▼
AI 调用 Tool: env_deploy_service (template=nacos-server)
  │
  ▼
MCP Server → EnvironmentService.deployService()
  │
  ▼
AI 调用 Tool: env_deploy_service (template=mysql)
  │
  ▼
AI 调用 Tool: test_health_check
  │
  ▼
AI 向用户汇报："环境就绪，Console: http://10.0.0.103:8848"
  │
  ▼
用户: "执行压测，100 连接 60 秒"
  │
  ▼
AI 调用 Tool: test_load
  (loadgen_host_id=vm-loadgen-01, target_url=http://10.0.0.103:8848/...)
  │
  ▼
MCP Server → LoadgenService 通过 SSH 在 vm-loadgen-01 执行 wrk
  │
  ▼
AI 调用 Tool: test_collect_metrics (env_id=env-xxx)
  │
  ▼
AI 向用户呈现：压测结果 + 资源指标
  │
  ▼
用户: "提交结论 ACCEPT"
  │
  ▼
AI 调用 Tool: session_conclude
  │
  ▼
MCP Server → EvidenceCollector 归档
```

### 3.2 错误处理数据流

```
test_health_check 失败
  │
  ▼
AI 调用 Tool: test_stream_logs (env_id, service_name=nacos-server, grep=ERROR)
  │
  ▼
AI 调用 Tool: test_exec_command (env_id, service_name=nacos-server, command="netstat -tlnp")
  │
  ▼
AI 向用户汇报：
  "健康检查失败。发现端口 8848 已被占用（进程 PID 1234）。
   建议：
   [A] 销毁现有环境重新部署（自动选择新端口）
   [B] 手动释放端口后重试
   [C] 指定其他端口覆盖部署"
```

---

## 4. 部署架构

### 4.1 独立进程模式（推荐）

```
┌─────────────────┐     SSE/HTTP      ┌──────────────────────┐
│   AI Client     │ ◄──────────────► │  MCP Server          │
│ (Claude/Trae)   │   Port 8081      │  (Spring Boot +      │
│                 │                  │   WebFlux + MCP SDK) │
└─────────────────┘                  └──────────────────────┘
                                              │
                                              │ REST / RSocket
                                              ▼
                                       ┌──────────────────────┐
                                       │  DevOps Dashboard    │
                                       │  Core API            │
                                       │  Port 8080           │
                                       └──────────────────────┘
```

### 4.2 同进程模式（简化部署）

```
┌─────────────────────────────────────────────────────────────┐
│              DevOps Dashboard Application                    │
│  ┌─────────────────┐                                        │
│  │  REST Controller│  ← /api/v1/* (现有前端/外部调用)        │
│  └─────────────────┘                                        │
│  ┌─────────────────┐                                        │
│  │  MCP Controller │  ← /mcp/sse (AI 专用)                  │
│  │  (MCP SDK)      │                                        │
│  └─────────────────┘                                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Service Layer (共享)                      │  │
│  │  HostService | EnvironmentService | LoadgenService ...  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 配置文件示例

```yaml
# application-mcp.yml
mcp:
  server:
    transport: sse          # sse | stdio | http
    path: /mcp/sse
    port: 8081            # 独立模式时有效

# hosts.yml — 主机拓扑注册表
hosts:
  - id: pve-01
    type: pve-hypervisor
    label: "PVE 虚拟化宿主机"
    network_zone: lan-10.0.0
    capabilities: [vm]
    roles: []
    resources: { cpu_total: 32, mem_total_mb: 128000 }

  - id: vm-fedora-dev
    type: vm
    parent: pve-01
    label: "Fedora 开发环境"
    network_zone: lan-10.0.0
    capabilities: [docker, native]
    roles: [mcp-host, target]
    resources: { cpu_total: 8, mem_total_mb: 16384 }
    access:
      ssh: 10.0.0.102:22
      user: dev
      key_path: ~/.ssh/id_rsa

  - id: vm-ubuntu-test
    type: vm
    parent: pve-01
    label: "Ubuntu 测试环境"
    network_zone: lan-10.0.0
    capabilities: [docker, native]
    roles: [target]
    resources: { cpu_total: 4, mem_total_mb: 8192 }
    access:
      ssh: 10.0.0.103:22
      user: test

  - id: vm-loadgen-01
    type: vm
    parent: pve-01
    label: "专用压测机"
    network_zone: lan-10.0.0
    capabilities: [native]
    roles: [loadgen]
    resources: { cpu_total: 8, mem_total_mb: 8192 }
    access:
      ssh: 10.0.0.104:22
      user: loadgen
    loadgen_tools: [wrk, hey, ab]
```

---

## 5. 安全边界

| 操作 | 是否需要确认 | 原因 |
|------|-------------|------|
| `env_create` | 是 | 占用资源 |
| `env_deploy_service` | 是 | 端口/资源变更 |
| `test_health_check` | 否 | 只读 |
| `test_load` | 是 | 消耗目标端和压测端资源 |
| `test_exec_command` | 是 | 任意命令执行风险 |
| `env_destroy` | 是 + 二次确认 | 不可逆，数据丢失 |
| `analyze_network_path` | 否 | 只读分析 |

---

## 6. 与现有系统的边界

```
┌────────────────────────────────────────┐
│  MCP Server (新增)                     │
│  • 负责: 协议适配、AI 交互、Host 管理   │
│  • 不直接操作: Docker、VM、SSH          │
└────────────────────────────────────────┘
              │
              │ 调用
              ▼
┌────────────────────────────────────────┐
│  Application Service (现有 + 少量新增) │
│  • HostService (新增)                   │
│  • LoadgenService (新增)                │
│  • EnvironmentService (现有)            │
│  • MonitoringService (现有)             │
│  • EvidenceCollector (新增)             │
└────────────────────────────────────────┘
              │
              │ 调用
              ▼
┌────────────────────────────────────────┐
│  Infrastructure (现有，完全不变)          │
│  • DockerComposeProvider               │
│  • SshCommandExecutor                  │
│  • TemplateRepository                  │
│  • EnvironmentRepository               │
└────────────────────────────────────────┘
```
