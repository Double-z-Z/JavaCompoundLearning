[mysqld]
server-id = 2
log-bin = mysql-bin
binlog_format = ROW

# 两个 MySQL 作为独立分片, 不做主从复制
# read_only 已在运行时关闭, 不再从 master 同步数据

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# 连接数
max_connections = 200
