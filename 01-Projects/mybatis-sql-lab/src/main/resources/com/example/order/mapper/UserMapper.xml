<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.mapper.UserMapper">

    <!-- ===== 基础 CRUD ===== -->

    <select id="selectById" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
    </select>

    <!--
        useGeneratedKeys + keyProperty: 将 PG 生成的自增 id 回填到 User 对象
        这是 MyBatis 获取自增主键的标准写法
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone)
        VALUES (#{username}, #{email}, #{phone})
    </insert>

    <update id="update">
        UPDATE users
        SET username = #{username},
            email = #{email},
            phone = #{phone}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!-- ===== 参数传递方式 ===== -->

    <!-- 单个基本类型：xml 中可用任意名字 -->
    <select id="selectByUsername" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
        WHERE username = #{任意名字都可以}
    </select>

    <!-- @Param 命名 -->
    <select id="selectByEmailAndPhone" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
        WHERE email = #{email} AND phone = #{phone}
    </select>

    <!-- Map 传参 -->
    <select id="selectByMap" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
        WHERE username = #{username} AND email = #{email}
    </select>

    <!-- ===== #{} vs ${} ===== -->

    <!--
        ${} 直接字符串拼接 —— 不预编译，不转义
        此处仅作为 ORDER BY 使用，调用方必须白名单校验
    -->
    <select id="selectAllOrderBy" resultType="com.example.order.model.User">
        SELECT id, username, email, phone, created_at
        FROM users
        ORDER BY ${column}
    </select>

    <!-- ===== 批量插入 ===== -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone) VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.username}, #{user.email}, #{user.phone})
        </foreach>
    </insert>

</mapper>
