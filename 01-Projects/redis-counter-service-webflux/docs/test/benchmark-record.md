---
tags:
  - redis
  - counter
  - architecture
  - benchmark
created: 2026-05-02
updated: 2026-05-08
---

# Redis 计数器服务压测报告

---

## 背景与目标

本次压测旨在验证 Redis 计数器服务的性能上限，识别瓶颈并确定优化方向。

## 测试环境

| 组件           | IP                                | 说明          |
| ------------ | --------------------------------- | ----------- |
| 网关           | 192.168.1.1                       | 家用路由器       |
| 外部压测端        | 192.168.1.2 (Windows + WSL)       | 8C16T       |
| PVE 宿主机      | WAN: 192.168.1.7 / LAN: 10.0.0.1  | OpenWrt NAT |
| 内部压测端        | 10.0.0.110 (Debian VM)            | 2C2GB，CPU受限 |
| 应用服务器        | 10.0.0.142:8080                   | Spring Boot |
| Redis Master | 10.0.0.104:6379 / 10.0.0.103:6379 | 被测系统        |

## 测试工具

- **Redis Benchmark**：单机基准测试，验证 Redis 服务端上限
- **wrk**：轻量 HTTP 压测，获取应用层基准（Linux/macOS）
- **bombardier**：轻量 HTTP 压测，wrk 的 Windows 替代方案（Go + fasthttp）
- **JMeter**：复杂场景压测，支持 HTML Report 生成
- **Jedis 直连测试**：绕过 HTTP 层，直接压测 Redis 客户端性能

---

## 核心数据

### Redis 服务端能力

| 测试路径 | QPS | 延迟 avg | 延迟 p99 | 说明 |
|----------|-----|----------|----------|------|
| PVE 内部 → Redis (SET) | 155,351 | 0.62ms | 0.99ms | 服务端真实上限 |
| PVE 内部 → Redis (GET) | 171,232 | 0.56ms | 1.09ms | 服务端真实上限 |
| PVE 内部 → Redis (INCR) | 157,529 | 0.61ms | 0.96ms | 服务端真实上限 |
| WSL 外部 → Redis (SET) | 43,084 | 1.38ms | 2.58ms | 受网络路径限制 |

### HTTP 应用层性能（JMeter，外部 Windows）

| 并发 | 样本数 | 错误率 | 平均响应 | 中位数 | P95 | P99 | 最大值 | 吞吐量 |
|------|--------|--------|----------|--------|-----|-----|--------|--------|
| 80 | 1,004,950 | 0% | 1.93ms | 2ms | 3ms | 3ms | 49ms | 33,622/s |
| 100 | 1,040,964 | 0% | 2.34ms | 2ms | 3ms | 4ms | 142ms | 34,788/s |
| 150 | 1,047,333 | 0% | 3.51ms | 4ms | 5ms | 7ms | 177ms | 35,010/s |

### HTTP 应用层性能（bombardier，预热后）

| 场景                  | 并发  | 样本数       | QPS        | P50 延迟     | P99 延迟 | 说明                                |
| ------------------- | --- | --------- | ---------- | ---------- | ------ | --------------------------------- |
| ping（纯 Tomcat）      | 100 | 1,784,182 | 59,493     | 1.25ms     | 8.00ms | 无 Redis 操作，HTTP 层理论上限             |
| decrement（完整链路）     | 100 | 1,164,835 | 38,841     | 2.27ms     | 9.91ms | Spring MVC，预热后稳定值，含 Redis Lua     |
| decrement（纯文本响应）    | 100 | 1,324,578 | 44,364     | 1.96ms     | 7.16ms | Spring MVC，移除 JSON 序列化，QPS +14.2% |
| decrement (WebFlux) | 100 | 1,778,019 | **59,284** | **1.55ms** | 5.37ms | 无服务线程阻塞，替代Tomcat                  |

> **关键发现**：
> 1. 首次测试仅 26,753 QPS，预热后提升至 38,841 QPS（+45%）。JVM C2 编译和连接池初始化对结果影响显著。
> 2. **纯文本响应优化验证成功**：QPS 从 38,841 提升至 44,364（+14.2%），P99 延迟从 9.91ms 降至 7.16ms（-27.7%）。
> 3. WebFlux 异步非阻塞架构带来显著性能提升，QPS 提升超 50%，延迟大幅降低。


### Redis 直连性能（Jedis，绕过 HTTP 层）

| 轮次 | 请求数 | QPS | 平均延迟 | 成功率 | 耗时 |
|------|--------|-----|----------|--------|------|
| 预热 | 50,000 | 3,828 | - | 100% | 13.06s |
| Round 1 | 500,000 | 89,847 | 1.100ms | 100% | 5.57s |
| Round 2 | 500,000 | 90,334 | 1.103ms | 100% | 5.54s |
| Round 3 | 500,000 | 89,111 | 1.120ms | 100% | 5.61s |
| **平均** | **1,500,000** | **89,764** | **1.108ms** | **100%** | **5.57s** |

> **测试方法**：使用 JedisCluster 直连 Redis，执行与生产环境完全相同的 Lua 脚本，绕过 Spring Boot + Tomcat + JSON 序列化。

### 扣减策略对比压测（2026-05-07）

| 并发 | 样本数 | 错误率 | 平均响应 | 中位数 | P95 | P99 | 最大值 | 吞吐量 | 策略 |
|------|--------|--------|----------|--------|-----|-----|--------|--------|------|
| 80 | 987,612 | 0% | 1.97ms | 2ms | 2ms | 3ms | 51ms | 32,994/s | raw |
| 80 | 1,004,950 | 0% | 1.93ms | 2ms | 3ms | 3ms | 49ms | 33,622/s | atomic |
> **关键发现**：lua脚本本身性能损耗可以忽略不计。

### 压测端对比

| 测试端 | 工具 | 并发 | QPS | 平均延迟 | CPU 规格 |
|--------|------|------|-----|----------|----------|
| 外部 Windows | JMeter | 80 | 33,622 | 1.93ms | 8C16T |
| 外部 Windows | bombardier | 100 | 38,841 | 2.27ms | 8C16T |
| 内部 Debian | JMeter | 80 | 21,741 | 2.82ms | 2C（受限） |
| 内部 Debian | wrk | 80 | 23,906 | 3.92ms | 2C（受限） |
| 外部 Windows | Jedis 直连 | 100 | 89,764 | 1.108ms | 8C16T |

---

## 关键发现

### 发现 1：服务稳定性优秀

JMeter 150并发、100万+样本压测全程 **0 错误率**，最大响应时间 177ms，P99 仅 7ms。证明服务在设计负载下运行可靠。

### 发现 2：吞吐量天花板约 35K/s

JMeter 并发从 80 增至 150，QPS 仅增长 4%（33,622 → 35,010）。服务在当前配置下接近上限。

### 发现 3：瓶颈在压测端 CPU，非服务端

外部 Windows JMeter（8C16T）达到 33,622 QPS，内部 Debian VM（2C）仅 21,741 QPS，**差距 35%**。iperf 测得网络带宽 950Mbps，排除网络瓶颈。

### 发现 4：应用层开销是主要差距来源（已精确定位）

通过分层压测，瓶颈已从"应用层"进一步精确定位到 **Spring MVC + JSON 序列化层**：

| 测试层次 | QPS | 占比 | 说明 |
|----------|-----|------|------|
| Redis 直连（Jedis） | 89,764 | 100% | 客户端 + Redis 理论上限 |
| ping（纯 Tomcat） | 59,493 | 66% | Spring MVC 调度开销 |
| HTTP decrement | 38,841 | 43% | 完整链路实际吞吐 |
| **Spring/JSON 损耗** | **51,064** | **57%** | **瓶颈所在** |

**关键证据**：
1. Redis 直连 89K vs HTTP 完整链路 38K，差值 51K（57% 性能被 HTTP 层消耗）
2. ping 测试可达 59K，证明 Tomcat 本身不是硬瓶颈
3. Lettuce 连接池调优无效（33K → 33.6K），排除连接池瓶颈

> 原结论"4.7x 差距来自 HTTP + JSON + Lettuce"已修正：Lettuce 不是瓶颈，**Spring MVC 路由调度 + JSON 序列化 + HashMap 创建** 才是主要开销。

### 发现 5：延迟长尾问题在并发升高时加剧

并发 80 时 P99 仅 3ms，150 时升至 7ms。平均响应时间从 1.93ms 增至 3.51ms（+82%），但中位数相对稳定（2ms → 4ms），说明主要是长尾在恶化。

---

## 瓶颈分层分析

```
┌─────────────────────────────────────────────────────────────┐
│ 第一层：压测端 CPU（内部VM 自身瓶颈）                           │
│   外部 Windows 33K vs 内部 VM 21K = 35% 性能差距              │
├─────────────────────────────────────────────────────────────┤
│ 第二层：网络路径（已排除）                                     │
│   iperf 950Mbps 远高于 Redis QPS 需求，NAT 无明显瓶颈         │
├─────────────────────────────────────────────────────────────┤
│ 第三层：HTTP 协议层（Spring MVC + JSON）                      │
│   ping 59K vs decrement 38K = 21K 差距（35% 损耗）           │
│   主要开销：路由调度、JSON 序列化、HashMap 创建                │
├─────────────────────────────────────────────────────────────┤
│ 第四层：Redis 客户端层（已排除）                               │
│   Jedis 直连 89K vs HTTP 38K = 51K 差距（57% 损耗）          │
│   Lettuce 连接池调优无效（33K → 33.6K），排除客户端瓶颈       │
├─────────────────────────────────────────────────────────────┤
│ 第五层：Redis 服务端（无瓶颈）                                 │
│   当前利用率 ~22%（38K / 155K），水平扩展可进一步提升          │
└─────────────────────────────────────────────────────────────┘
```

---

## 实践经验总结

### 1. 压测端选择

| 场景 | 推荐工具 | 原因 |
|------|----------|------|
| 复杂 HTTP 场景 | JMeter | 支持 cookies、headers、POST body |
| 快速基准测试 | wrk | 轻量，单文件，Lua 脚本扩展 |
| 外部压测 | 使用高配物理机 | VM CPU 受限会影响压测准确性 |

**结论**：压测端 CPU 规格直接影响压测结果上限，应使用高配物理机而非 VM。

### 2. 测试位置选择

| 测试方式 | 适用场景 | 注意事项 |
|----------|----------|----------|
| 同物理机测试 | 快速验证 | 压测工具和服务共享资源，互相影响 |
| 内部网络测试 | 基线验证 | 排除网络因素，专注服务端能力 |
| 外部压测 | 真实场景 | 最接近生产环境，但需排除压测端瓶颈 |

**结论**：服务端能力测试用内部网络，性能验收测试用外部高配物理机。

### 3. 瓶颈分析方法（分层排除法）

```
问题：QPS 低于预期
↓
排查顺序：压测端 CPU → 网络带宽 → HTTP 协议层 → Redis 客户端 → Redis 服务端
↓
验证方法：
  1. iperf 测带宽
  2. VM 对比排除 CPU 瓶颈
  3. ping 接口测 HTTP 基线（排除 Redis）
  4. Jedis 直连测客户端层（排除 HTTP）
  5. redis-benchmark 测服务端上限
```

**关键教训**：
- **预热效应不可忽略**：首次测试 26K vs 预热后 38K（+45%），JVM C2 编译和连接池初始化需要时间
- **工具选择影响结果**：JMeter 33K vs bombardier 38K，客户端自身性能也会成为瓶颈
- **分层测试才能定位**：单纯对比 Redis Benchmark 和 HTTP QPS 无法区分是 HTTP 层还是 Redis 客户端层的问题

---

## 后续计划

| 优化项 | 预期收益 | 实际收益 | 优先级 | 状态 | 说明 |
|--------|----------|----------|--------|------|------|
| ~~Lettuce 连接池调优~~ | ~~20-30%~~ | - | ~~P1~~ | ❌ 已排除 | 调优无效（33K → 33.6K），非瓶颈 |
| 纯文本响应（移除 JSON） | 10-20% | **+14.2%** | P0 | ✅ 已完成 | QPS 38K → 44K，P99 9.9ms → 7.2ms |
| HTTP Pipeline | - | - | ❌ 已排除 | 不适用于浏览器用户场景 |
| Lua 脚本优化 | - | - |  ❌ 已排除 | 已验证非瓶颈 |
| 异步框架（WebFlux） | 50%+ | **+52.6%** | P2 | ✅ 已完成 | 非阻塞 IO，QPS 38K → 59K，P99 9.9ms → 5.4ms |
| Redis 水平扩展 | 线性增长 | - | P2 | 📋 规划中 | 主从 + 分片 |

---

## 命令速查

### 初始化库存

```bash
curl -X POST http://10.0.0.142:8080/stock/SKU001/init?quantity=1000000000
```

### Redis Benchmark

```bash
# PVE 内部测试（服务端上限）
redis-benchmark -h 10.0.0.104 -p 6379 -t set -n 1000000 -c 100 -d 10
```

### WRK 压测

```bash
# post.lua
echo 'wrk.method = "POST"' > ~/workspace/post.lua

# 压测 decrement 接口
wrk -t4 -c100 -d30s -s ~/workspace/post.lua http://10.0.0.142:8080/stock/SKU001/decrement?quantity=1
```

### JMeter

```powershell
$ts = Get-Date -Format "yyyyMMdd-HHmmss"
jmeter -n -t .\stock_decr.jmx `
    -l "benchmark_results_$ts.jtl" `
    -e -o "benchmark_report_$ts" `
    -J "benchmark_log_$ts"
```

参数说明：`-n` 非GUI模式，`-t` 测试计划文件，`-l` 结果文件，`-j` 日志文件（时间戳区分不同运行）。

---

### bombardier（Windows 推荐）

```powershell
# 下载
Invoke-WebRequest -Uri "https://github.com/codesenberg/bombardier/releases/download/v2.0.2/bombardier-windows-amd64.exe" -OutFile "bombardier.exe"

# ping 测试（HTTP 基线）
.\bombardier.exe -c 100 -d 30s http://10.0.0.142:8080/stock/ping

# decrement 测试（POST 请求）
.\bombardier.exe -c 100 -d 30s -l -m POST http://10.0.0.142:8080/stock/SKU001/decrement?quantity=1
```

### Jedis 直连测试

```bash
# 编译
cd tools
javac -encoding UTF-8 -cp "jedis-5.0.0.jar;commons-pool2-2.12.0.jar;slf4j-api-1.7.36.jar" RedisDirectBenchmark.java

# 运行
java -cp ".;jedis-5.0.0.jar;commons-pool2-2.12.0.jar;slf4j-api-1.7.36.jar" RedisDirectBenchmark
```

---

*原文档位置：`01-Projects/redis-counter-service/docs/压测记录.md`*
*迁移日期：2026-05-12*
