---
created: 2026-05-02
updated: 2026-05-03
tags: [project, redis, seckill, stock, lua, performance, benchmark]
status: in-progress
mastery: 65
---

# 高并发计数器服务 - 秒杀库存扣减

> 项目目标：通过真实压测理解 Redis 在高并发场景下的能力边界
> 项目类型：综合应用型
> 场景：双11/618 秒杀活动、商品库存扣减


## 实现状态

| 组件 | 状态 | 说明 |
|-----|------|-----|
| StockService.java | ✅ 完成 | 服务接口 |
| StockServiceImpl.java | ✅ 完成 | 实现类，含 Lua 脚本 |
| StockController.java | ✅ 完成 | REST API |
| 单元测试 | ✅ 完成 | 超卖防护验证通过 |

---

## 核心设计

### Lua 脚本（原子扣减）
```lua
local stock = tonumber(redis.call('GET', KEYS[1]) or 0)
local quantity = tonumber(ARGV[1])
if stock >= quantity then
    return redis.call('DECRBY', KEYS[1], quantity)
else
    return -1
end
```

### 业务流程
```
用户下单 → 库存扣减 → Redis Lua 原子操作 → 成功/库存不足
```

### 设计决策
- **库存用 Redis**：抵挡秒杀高并发写请求
- **Lua 保证原子性**：避免并发超卖
- **最终一致性**：异步同步到 MySQL

## 项目完成里程碑

| 阶段 | 状态 | 完成时间 | 说明 |
|------|------|----------|------|
| Phase 0: 环境搭建 | ✅ 完成 | 2026-05-02 | Redis Cluster + 应用服务 |
| Phase 1: 核心功能 | ✅ 完成 | 2026-05-03 | 库存扣减 + Lua 脚本 |
| Phase 2: 压测验证 | ✅ 完成 | 2026-05-06 | JMeter 100万样本，0错误率 |
| Phase 3: 优化迭代 | 🔄 进行中 | - | HTTP Pipeline 实施中 |

## 后续优化计划

| 优化项 | 预期收益 | 优先级 | 状态 |
|--------|----------|--------|------|
| HTTP Pipeline | 5-6x | P0 | 🔄 实施中 |
| Lettuce 连接池调优 | 20-30% | P1 | ⏳ 待开始 |
| Lua 脚本优化 | 10-20% | P1 | ⏳ 待开始 |
| 异步框架（WebFlux） | 50%+ | P2 | 📋 规划中 |
| Redis 水平扩展 | 线性增长 | P2 | 📋 规划中 |

**下一步**：实施 HTTP Pipeline，验证应用层性能提升。

---

## 架构决策

### 扩展策略
- **不要追求高配单机**：单线程无法利用多核
- **水平扩展**：3 节点 Cluster ≈ 60 万 QPS
- **Pipeline**：可提升 5-6 倍吞吐量（需要客户端支持）

### 容量估算
```
目标 QPS ÷ 20万 ≈ 所需节点数

示例：50 万 QPS → 3 节点
示例：100 万 QPS → 5 节点
```

### 成本效益
| 节点规格 | 单节点 QPS | 成本/月 |
|---------|-----------|--------|
| 1核 1GB | ~20万 | $6 |
| 2核 2GB | ~20万 | $12 |
| 物理机 | 可能更高 | 视配置 |

---

## 测试方法论

### 关键教训
1. **压测端也可能成为瓶颈**：开发机/客户端网络栈
2. **PVE 内部存在资源竞争**：多 VM 同时压测结果不稳定
3. **从外部网络测试更可靠**：避免 VM 间资源竞争
4. **本地压测与远程压测结果不同**：网络路径和客户端性能差异

### 推荐的测试方法
1. 从外部机器（非 PVE 内）发起压测
2. 多台客户端分散负载
3. 使用 Pipeline 模式提升客户端效率

---
📊 **项目完成度**: 75%
🎯 **核心收获**:
- Redis 服务端上限 15.5 万 QPS（P99 < 1ms）
- HTTP 应用层实际吞吐 3.5 万 QPS（P99 7ms）
- 瓶颈在应用层而非 Redis 服务端
- JMeter 100万样本 0 错误率验证服务可靠性

🔗 **关联练习数**: 5（Phase1~3 完成）
📈 **知识点掌握度**:
- [[Redis-Lua脚本]]: 🍎 应用（验证通过）
- [[Redis-String]]: 🍎 70
- [[Redis-性能压测]]: 🌿🌿 理解（通过压测验证）
- [[Lettuce]]: 🌱 初识（连接开销实测）