# Java复利学习工程 — AI Agent 指南

> 本项目是一个基于**复利思维 + AI协作 + Obsidian知识管理**的个人深度学习框架。
>
> 💡 **双平台支持**：兼容 **Trae IDE** 和 **Claude Code**，共享数据层 `.agent/config.md` + `.agent/profile.md`

---

## 1. 项目概述

### 核心理念

1. **复利学习思维**：每个新知识必须链接到已有知识，形成网络
2. **Obsidian + AI 知识库**：Obsidian 负责可视化，AI 负责智能推荐与连接
3. **人制定计划，AI 负责推荐**：人决定学什么，AI 基于数据给出建议并记录执行

---

## 2. 全局加载约束（所有 Skill 必须遵守）

### 2.1 必读（每次对话）

| 文件 | 范围 | 用途 |
|------|------|------|
| `.agent/config.md` | 全文 | 路径字典，本 Session 不重复读取 |
| `.agent/profile.md` | 仅 3 个章节 | 坐标/边界/上下文 |
| `「当前快照」` | | 当前 GOAL/EMRG 状态 |
| `「拒绝清单」` | | 边界过滤 |
| `「环境默认值」` | | 上下文 |

### 2.2 强制检查（任何推荐前）

- 检查 `「拒绝清单」`，命中则过滤并说明原因
- 检查 GOAL 的 `deadline`，过期则提示归档

### 2.3 加载上限

- **单次对话加载不超过 5 个文件**（防止上下文腐烂）
- **单次 Agent/工具调用扫描不超过 10 个文件**（禁止大面积递归搜索）
- 优先读 frontmatter 字段（status/mastery/deadline），正文按需切片
- 禁止预扫描全库知识（按需检索）

---